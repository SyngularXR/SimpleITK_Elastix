name: Release Elastix Wheels

on:
  release:
    types: [published]
  workflow_dispatch:

concurrency:
  group: '${{ github.workflow }}@${{ github.sha }}'

permissions:
  contents: write

jobs:
  build-linux-x86_64:
    runs-on: [self-hosted, Linux, X64]
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Clean workspace
        run: git clean -fdx

      - name: Build Docker Image
        working-directory: Utilities/Distribution/manylinux
        run: |
          docker build --pull=true --rm=true -t simpleitk_manylinux -f Dockerfile-2014-x86_64 .

      - name: Build Elastix Wheels
        env:
          PYTHON_VERSIONS: "cp310-cp310"
          BUILD_CSHARP: 0
          BUILD_JAVA: 0
          BUILD_PYTHON_LIMITED_API: 1
          SIMPLEITK_USE_ELASTIX: "ON"
        run: |
          docker run --rm \
            --user "$(id -u):$(id -g)" \
            --env PYTHON_VERSIONS \
            --env BUILD_CSHARP \
            --env BUILD_JAVA \
            --env BUILD_PYTHON_LIMITED_API \
            --env SIMPLEITK_USE_ELASTIX \
            --env SIMPLEITK_SRC_DIR="/work/src" \
            -v "${{ github.workspace }}:/work/src" \
            -v "${{ github.workspace }}/Utilities/Distribution/manylinux:/work/io" \
            -t simpleitk_manylinux

      - name: List Outputs
        run: |
          find "${{ github.workspace }}/Utilities/Distribution/manylinux" -name "*.whl" -o -name "*.log" | head -50

      - name: Smoke Test Elastix
        run: |
          WHL=$(find "${{ github.workspace }}/Utilities/Distribution/manylinux/wheelhouse" -name "*.whl" | head -1)
          pip install --user "$WHL"
          python3 -c "import SimpleITK as sitk; f = sitk.ElastixImageFilter(); print('Elastix OK')"
          pip uninstall -y simpleitk

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: linux-x86_64-wheels
          path: |
            ${{ github.workspace }}/Utilities/Distribution/manylinux/wheelhouse/*.whl

  build-windows:
    runs-on: [self-hosted, Windows, X64]
    timeout-minutes: 360
    defaults:
      run:
        shell: cmd
    env:
      BLD_DIR: "${{ github.workspace }}\\bld"
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Clean workspace
        run: git clean -fdx

      - name: Set up MSVC
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: amd64

      - name: Install Ninja
        run: pip install ninja

      - name: SuperBuild with Elastix
        run: |
          cmake -G "Visual Studio 17 2022" -A x64 ^
            -DSimpleITK_BUILD_DISTRIBUTE:BOOL=ON ^
            -DBUILD_TESTING:BOOL=OFF ^
            -DBUILD_EXAMPLES:BOOL=OFF ^
            -DBUILD_SHARED_LIBS:BOOL=OFF ^
            -DWRAP_DEFAULT:BOOL=OFF ^
            -DSimpleITK_USE_ELASTIX:BOOL=ON ^
            -DITK_C_OPTIMIZATION_FLAGS:STRING="" ^
            -DITK_CXX_OPTIMIZATION_FLAGS:STRING="" ^
            -S "%GITHUB_WORKSPACE%\SuperBuild" ^
            -B "%BLD_DIR%"
          cmake --build "%BLD_DIR%" --config Release

      # --- Limited API wheel (covers Python 3.11, 3.12, 3.13) ---
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Build cp311-abi3 Wheel
        run: |
          for /f "delims=" %%i in ('python -c "import sys; print(sys.executable)"') do set PYTHON_EXE=%%i
          set VENV_PYTHON=%BLD_DIR%\venv\Scripts\python.exe
          if not exist "%VENV_PYTHON%" set VENV_PYTHON=%PYTHON_EXE%
          cmake -G Ninja ^
            -DCMAKE_PREFIX_PATH:PATH="%BLD_DIR%" ^
            -DCMAKE_BUILD_TYPE:STRING=Release ^
            -DSWIG_EXECUTABLE:FILEPATH="%BLD_DIR%\swigwin\swig.exe" ^
            -DSWIG_DIR:PATH="%BLD_DIR%\swigwin" ^
            -DSimpleITK_PYTHON_USE_LIMITED_API:BOOL=ON ^
            -DSimpleITK_BUILD_DISTRIBUTE:BOOL=ON ^
            -DSimpleITK_PYTHON_WHEEL:BOOL=ON ^
            -DSimpleITK_Python_EXECUTABLE:FILEPATH="%VENV_PYTHON%" ^
            -DPython_EXECUTABLE:FILEPATH="%PYTHON_EXE%" ^
            -S "%GITHUB_WORKSPACE%\Wrapping\Python" ^
            -B "%GITHUB_WORKSPACE%\py311-abi3"
          cmake --build "%GITHUB_WORKSPACE%\py311-abi3" --config Release
          cmake --build "%GITHUB_WORKSPACE%\py311-abi3" --config Release --target dist

      # --- Python 3.10 wheel ---
      - name: Set up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Build cp310 Wheel
        run: |
          for /f "delims=" %%i in ('python -c "import sys; print(sys.executable)"') do set PYTHON_EXE=%%i
          set VENV_PYTHON=%BLD_DIR%\venv\Scripts\python.exe
          if not exist "%VENV_PYTHON%" set VENV_PYTHON=%PYTHON_EXE%
          cmake -G Ninja ^
            -DCMAKE_PREFIX_PATH:PATH="%BLD_DIR%" ^
            -DCMAKE_BUILD_TYPE:STRING=Release ^
            -DSWIG_EXECUTABLE:FILEPATH="%BLD_DIR%\swigwin\swig.exe" ^
            -DSWIG_DIR:PATH="%BLD_DIR%\swigwin" ^
            -DSimpleITK_PYTHON_USE_LIMITED_API:BOOL=OFF ^
            -DSimpleITK_BUILD_DISTRIBUTE:BOOL=ON ^
            -DSimpleITK_PYTHON_WHEEL:BOOL=ON ^
            -DSimpleITK_Python_EXECUTABLE:FILEPATH="%VENV_PYTHON%" ^
            -DPython_EXECUTABLE:FILEPATH="%PYTHON_EXE%" ^
            -S "%GITHUB_WORKSPACE%\Wrapping\Python" ^
            -B "%GITHUB_WORKSPACE%\py310"
          cmake --build "%GITHUB_WORKSPACE%\py310" --config Release
          cmake --build "%GITHUB_WORKSPACE%\py310" --config Release --target dist

      - name: Collect Wheels
        run: |
          mkdir "%GITHUB_WORKSPACE%\artifacts" 2>nul
          for /r "%GITHUB_WORKSPACE%\py311-abi3" %%f in (SimpleITK*.whl) do copy "%%f" "%GITHUB_WORKSPACE%\artifacts\"
          for /r "%GITHUB_WORKSPACE%\py310" %%f in (SimpleITK*.whl) do copy "%%f" "%GITHUB_WORKSPACE%\artifacts\"

      - name: Smoke Test Elastix
        run: |
          for %%f in ("%GITHUB_WORKSPACE%\artifacts\*.whl") do set WHL=%%f
          pip install --user "%WHL%"
          python -c "import SimpleITK as sitk; f = sitk.ElastixImageFilter(); print('Elastix OK')"
          pip uninstall -y simpleitk

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: windows-x86_64-wheels
          path: ${{ github.workspace }}/artifacts/*.whl

  publish:
    needs: [build-linux-x86_64, build-windows]
    runs-on: ubuntu-latest
    if: github.event_name == 'release'
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: actions/download-artifact@v4
        id: download
        with:
          path: ${{ github.workspace }}/artifacts

      - name: Checksums
        shell: bash
        run: |
          find ${{ steps.download.outputs.download-path }} -type f -name "*.whl" \
            | xargs sha256sum | tee checksums.txt

      - name: Upload Wheels to Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="${{ github.event.release.tag_name }}"
          echo "Uploading wheels to release: $TAG"
          gh release upload "$TAG" \
            $( find ${{ steps.download.outputs.download-path }} -type f -name "*.whl" ) \
            checksums.txt \
            --clobber

      - name: Cleanup Old Releases (keep latest 20)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release list --limit 100 --json tagName,createdAt \
            --jq 'sort_by(.createdAt) | reverse | .[20:] | .[].tagName' | \
          while read -r tag; do
            echo "Deleting old release: $tag"
            gh release delete "$tag" --yes || true
          done
